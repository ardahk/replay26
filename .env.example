TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_TASK_QUEUE=brewery-task-queue
RUNTIME_DIR=.runtime

# Temporal worker: POST JSON payloads when fermentation alarms fire or manual QA tasks are created.
# Leave unset to skip outbound HTTP (logs still go to worker stdout).
# ALARM_WEBHOOK_URL=https://hooks.example.com/services/YOUR/WEBHOOK
